{
  "name": "qntoy",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Seeded qutrit-inspired audio-visual field toy using Web Audio API and Three.js.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/audio.js && node --check src/visualizer.js && node --check src/logger.js && node --check src/app.js"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "6.3.5"
  },
  "engines": {
    "node": ">=20"
  }
}
